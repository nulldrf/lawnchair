<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/preference_scaffold_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        Layer 1 — blurred wallpaper bitmap. First child = lowest z-order.
        Set to GONE when blur is disabled.
    -->
    <ImageView
        android:id="@+id/preference_blur_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!--
        Layer 2 — adaptive theme scrim. Background colour is set at runtime to
        colorSurface at ~70% alpha. In dark mode colorSurface is near-black so
        the wallpaper dims; in light mode it is near-white so it brightens.
        Set to GONE when blur is disabled.
    -->
    <View
        android:id="@+id/preference_blur_scrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!--
        Layer 3 — AppBarLayout. Background set to null at runtime when blur is
        on so it draws nothing and the layers below show through the full height
        of the screen including the top bar area.
        app:liftOnScroll="false" disables LiftOnScrollHelper which would
        otherwise re-apply MaterialShapeDrawable fill on scroll events.
    -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/preference_appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        android:stateListAnimator="@null"
        app:liftOnScroll="false">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/preference_collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="152dp"
            app:collapsedTitleTextAppearance="?textAppearanceTitleLarge"
            app:collapsedTitleTextColor="?attr/colorOnSurface"
            app:contentScrim="?attr/colorSurfaceContainer"
            app:expandedTitleMarginBottom="24dp"
            app:expandedTitleMarginStart="24dp"
            app:expandedTitleTextAppearance="?textAppearanceHeadlineLarge"
            app:expandedTitleTextColor="?attr/colorOnSurface"
            app:layout_scrollFlags="scroll|exitUntilCollapsed|snap"
            app:statusBarScrim="@android:color/transparent"
            app:titleCollapseMode="scale">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/preference_toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@android:color/transparent"
                android:elevation="0dp"
                android:paddingStart="4dp"
                android:paddingEnd="8dp"
                app:layout_collapseMode="pin"
                app:navigationIconTint="?colorOnSurface">

                <FrameLayout
                    android:id="@+id/preference_toolbar_actions"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:layout_gravity="end" />

            </com.google.android.material.appbar.MaterialToolbar>

        </com.google.android.material.appbar.CollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <app.lawnchair.ui.preferences.components.layout.StretchNestedScrollView
        android:id="@+id/preference_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:clipToPadding="false"
        android:overScrollMode="always"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">

        <FrameLayout
            android:id="@+id/preference_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </app.lawnchair.ui.preferences.components.layout.StretchNestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
